# Common ORM decorators for TypeSpec database emitters.

This package contains common decorators for TypeSpec database emitters. These decorators are used by the various database emitters to generate the appropriate code for the target database. This package is not intended to be used directly by users, but rather as a dependency for the various database emitters.
